replicaCount: 1

image:
  repository: ghcr.io/kiukairor/pulse-feed
  pullPolicy: IfNotPresent
  tag: "sha-26348fa"

imagePullSecrets:
  - name: ghcr-secret

service:
  type: ClusterIP
  port: 3001

ingress:
  enabled: false

env:
  NEW_RELIC_APP_NAME: "pulse-pulse-feed"
  HOSTNAME: "0.0.0.0"
  PORT: "3001"
  NEXT_PUBLIC_EVENT_SVC_URL: "https://event.pulse.test:30443"
  NEXT_PUBLIC_AI_SVC_URL: "https://ai.pulse.test:30443"

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "500m"

secrets:
  newrelic: newrelic-secret
  postgres: postgres-secret
  redis: redis-secret
  anthropic: anthropic-secret
  app: app-secret
