name: Build and Deploy — pulse-shell

on:
  push:
    branches: [main]
    paths:
      - 'frontends/pulse-shell/**'
      - '.github/workflows/pulse-shell.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontends/pulse-shell/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/pulse-shell

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./frontends/pulse-shell
          platforms: linux/arm64   # Raspberry Pi
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_EVENT_SVC_URL=https://event.pulse.test:30443
            NEXT_PUBLIC_AI_SVC_URL=https://ai.pulse.test:30443
            NEXT_PUBLIC_FEED_MFE_URL=https://feed.pulse.test:30443
            NEXT_PUBLIC_PROFILE_MFE_URL=https://profile.pulse.test:30443

  update-image-tag:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Helm values with new image tag
        run: |
          SHA_TAG="sha-$(echo ${{ github.sha }} | cut -c1-7)"
          sed -i "s/tag: .*/tag: \"${SHA_TAG}\"/" infra/helm/pulse-shell/values.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add infra/helm/pulse-shell/values.yaml
          git commit -m "ci: update pulse-shell image tag to ${SHA_TAG} [skip ci]"
          git pull --rebase origin main
          git push
